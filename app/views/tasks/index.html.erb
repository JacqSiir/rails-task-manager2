<h1>Tasks</h1>
<h4><%= link_to "New task", new_task_path %></h4>
<ul>
  <% @tasks.each do |task|%>
    <li><h2><%= link_to task.title, task_path(task) %></h2></li>
      <ul>
        <li><%= task.details %></li>
        <li><%= task.completed %></li>
      </ul>
  <% end %>
</ul>
